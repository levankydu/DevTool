<UserControl x:Class="PathwayDevTool.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodels="clr-namespace:PathwayDevTool.ViewModels" 
             xmlns:models="clr-namespace:PathwayDevTool.Models"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">

    <Menu Background="#F5F5F5" Padding="2">
        <MenuItem Header="_File">
            <MenuItem Header="{Binding LoadMenuText}" 
          Command="{Binding LoadCommand}"
          InputGestureText="Ctrl+O">
                <MenuItem.Icon>
                    <TextBlock Text="📁" FontSize="14"/>
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="_Remember Last Path" 
                      IsCheckable="True"
                      IsChecked="{Binding SaveRecentPaths}">
                <MenuItem.Icon>
                    <TextBlock Text="💾" FontSize="14"/>
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="E_xit" 
                      Command="{Binding ExitCommand}"
                      InputGestureText="Alt+F4">
                <MenuItem.Icon>
                    <TextBlock Text="❌" FontSize="14"/>
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>

        <MenuItem Header="_Services">
            <MenuItem Header="Start _All" 
                      Command="{Binding StartAllCommand}">
                <MenuItem.Icon>
                    <TextBlock Text="▶" FontSize="14" Foreground="Green"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Stop A_ll" 
                      Command="{Binding StopAllCommand}">
                <MenuItem.Icon>
                    <TextBlock Text="■" FontSize="14" Foreground="Red"/>
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>

        <MenuItem Header="_Help">
            <MenuItem Header="_About" 
                      Command="{Binding AboutCommand}"
                      InputGestureText="F1">
                <MenuItem.Icon>
                    <TextBlock Text="ℹ" FontSize="14"/>
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
    </Menu>
</UserControl>